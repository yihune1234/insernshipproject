- [NID Simulation Mode](nid-simulation.md) — NID service has built-in simulation; no external server needed in dev; `NID_SIMULATION_MODE=True` by default when `DJANGO_ENV != production`.
- [Test Users & FINs](test-users.md) — Standard test accounts + canonical NID FINs for all three layers; run `create_test_users` after every fresh migrate.
- [Django Settings Layout](django-settings.md) — Single `base.py` with env-driven overrides; `ENVIRONMENT` var drives CORS/cache/debug; LocMemCache used when no Redis; production guards raise ImproperlyConfigured.
- [Token Blacklist Migrations](token-blacklist.md) — `rest_framework_simplejwt.token_blacklist` requires 12 migrations; must run `manage.py migrate` after first adding to INSTALLED_APPS.
- [DID Model Fields](did-model-fields.md) — DID uses `did_string` (not `did`) and `is_published` (not `is_primary`); DIDKey.did_document expects DIDDocument not DID; DID and DIDDocument are separate models.
- [API Endpoint Prefix Map](api-endpoint-map.md) — Spec-aligned URL layout: /api/auth/, /api/national-id/, /api/did/, /api/wallet/, /api/issuance/, /api/templates/, /api/issuer/, /api/verifier/; legacy /api/dids/ and /api/organizations/ kept as aliases.
- [Backend Layout](backend-layout.md) — Active backend is `backend/` (not backend2/); `backend/apps/` holds thin routing apps; `sys.path.insert(0, APPS_DIR)` keeps module names clean; workflow binds to `0.0.0.0:8000` (not localhost).
- [Holder Frontend Pages](holder-frontend.md) — All 10 holder pages fully rewritten; shares list uses `list` action (not `get_share_link`); delete notification path is `/wallet/notifications/<id>/delete/`; Textarea has no `hint` prop.
- [Issuer Integration](issuer-integration.md) — Full issuer portal wired; FileUpload uses onDrop/files/onRemove (not onFilesChange); IssuerRegDocument is db_table='registration_documents_v2'; do NOT name it RegistrationDocument (conflicts with org model).
- [Federated Member Service](federated-member-service.md) — FederatedMemberService calls org's external API; 7 distinct exceptions; dot-notation field resolution; PendingCredential auto-delivers on NID verify signal; mock_org_api/ runs on port 3001.
- [Model-Migration Drift](model-migration-drift.md) — Organization and CredentialType models were rewritten without matching migrations; required RunSQL migrations to make orphaned NOT NULL columns nullable.
- [Mobile API Path Corrections](mobile-api-paths.md) — Mobile uses `/wallet/` prefix; several endpoints were missing from backend (shares/enable, shares/disable, shares/stats, my-credentials alias, request-catalog) and were added.
